{
  "name": "agent-dev",
  "version": "0.2.0",
  "private": true,
  "description": "Agentic product delivery framework",
  "type": "module",
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.8.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n daemon,studio -c cyan,magenta \"npm run dev -w @agent-dev/daemon\" \"npm run dev -w @agent-dev/studio\"",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "doctor": "tsx -e \"import { runDoctor, formatDoctorSummary } from './packages/agent-runtime/src/doctor.ts'; const report = await runDoctor(); console.log(formatDoctorSummary(report));\"",
    "install:macos": "bash scripts/install-macos.sh",
    "update": "bash scripts/update.sh",
    "start": "npm run start -w @agent-dev/cli",
    "mcp": "npm run mcp -w @agent-dev/cli"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "concurrently": "^9.1.2",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2",
    "wrangler": "^4.120.0"
  }
}
