# 安全与代码质量审计（2026-08-31）

> 状态：发现已确认；**§6.1 P0 网络边界全部 4 项与 §6.2 P1 状态机全部 3 项已于 2026-08-31 修复并测试验证**，其余整改未开始。整改完成后请在「整改方案」逐项标记并同步 [handoff](../handoff.md)。
> 范围：全仓（`apps/cli`、`apps/daemon`、`apps/studio` 与 `packages/` 下 8 个包）。
> 方法：三路并行——客观检查（`npm run typecheck` / `npm test`）、安全审计（凭证、注入、网络边界、MCP 边界）、代码质量审计（死代码、类型纪律、错误处理、测试覆盖、文档一致性）；安全审计的高危结论均已逐一复核源码。

## 1. 总体结论

核心链路质量良好：状态机、凭证主路径、MCP 桥接、确认字面量防伪机制与 `docs/mcp-bridge.md` 的设计一致，类型纪律克制（无真实 `any`/`@ts-ignore`），所有子进程调用均为参数数组、无 shell 拼接，全库无硬编码密钥。

**真正的缺口在网络边界**：daemon 以零鉴权监听所有网卡，使"确认字面量""人工审批门""凭证不出本机"等上层设计在网络层面全部失效——存在至少三条独立的未认证远程代码执行/凭证读取路径。这是外部 Pilot 的阻断项，必须在本文件 §6.1 的整改完成前避免把安装脚本分发给外部用户。

## 2. 客观检查

| 项目   | 结果                                                                 |
| ---- | ------------------------------------------------------------------ |
| 类型检查 | ✅ 9 个工作区全部通过，零错误                                                   |
| 测试   | ⚠️ 199 例中 196 通过；3 个失败均为 Windows 平台问题（本机为 Windows 开发机，目标平台为 macOS） |

3 个失败与根因：

1. `packages/storage/test/index.test.ts:414`——质量门误判 `failed`。**真问题（跨平台）**：`packages/storage/src/index.ts:777` 用 `execFileAsync('npm', ['run', 'quality'])` 但未加 `shell: true`，Windows 上 `npm` 是 `npm.cmd`，直接 `ENOENT` 后被 catch 判为失败。同类写法在 `:844`（`npm install`）与 `packages/agent-runtime/src/doctor.ts` 的多处诊断调用（`npm`/`npx`/`gh`/`vercel`）。
2. `packages/agent-runtime/test/index.test.ts:51`——期望超时强杀后收到 `SIGKILL`，POSIX 专属信号语义，Windows 无此行为。
3. `packages/storage/test/index.test.ts:245`——用例需创建符号链接验证「外部 symlink 拒绝提交」，Windows 无开发者模式/管理员权限时创建本身 `EPERM`。

## 3. 安全发现

### 3.1 高危（4 条，全部复核确认）

共同特征：未认证 + 网络可达。只要第 1 条存在，其余各条都升级为远程可利用。

| #  | 发现                                                                                                                   | 出处                                                                                           | 影响                        |
| -- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------------------- |
| S1 | daemon 无鉴权监听所有网卡。`serve()` 未传 hostname，默认 `0.0.0.0`；`app.ts` 全文无 auth 中间件                                            | `apps/daemon/src/index.ts:45`、`apps/daemon/src/app.ts`                                       | 同网段任意主机可读写凭证、触发部署与发布      |
| S2 | 未认证 RCE（自动更新）。`POST /api/update` 直接 `git pull` + `npm install` + `npm run build`                                     | `apps/daemon/src/app.ts:257-279`                                                             | 任何可达 3737 端口者可植入恶意依赖/代码   |
| S3 | 仓库导入 URL 可执行任意命令。`importRepositoryUrl` 仅 `z.string().url()`（不限 scheme），原样传给 `git clone`；git 的 `ext::` 协议会执行 URL 中的命令 | `apps/daemon/src/app.ts:41`、`:608`                                                           | 未认证 RCE；`file://` 亦可读本地仓库 |
| S4 | 凭证明文外泄通道。`GET /api/secret-backend/:key` 无鉴权返回 secret 全文及全部历史版本                                                       | `apps/daemon/src/app.ts:330-341`、`packages/provider-cli/src/secret-backend/local-file.ts:59` | 直接违反"凭证不出本机"设计            |

连带风险：因为没有任何鉴权，本机浏览器访问恶意网页时也可发起 CSRF 式请求——CORS（`app.ts:175`，仅限 `http://localhost:5173`）只挡"读"不挡"写"，非预检 POST（如 `text/plain` 体）与带副作用的 GET（`/api/update/check` 会执行 `git fetch`）都能被跨站触发。

### 3.2 中危

| #  | 发现                                                                                                                    | 出处                                                                 |
| -- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| S5 | 审批门只验确认字面量 + 任意 `approvedBy` 字符串，无身份/会话校验；任何本地进程或 LAN 对端可伪造"人工审批"                                                     | `app.ts:563`、`:927`、`:1024`、`:1396` 等                              |
| S6 | `POST /api/runtime/catalog` 无鉴权可注册任意 `launchCommand`，探测时 `spawnSync(command, ['--version'])` 会执行之                     | `app.ts:743-750`、`packages/agent-runtime/src/catalog.ts:93`        |
| S7 | 存储型 XSS：`z.string().url()` 接受 `javascript:`，PR 证据 URL 直接渲染为 `<a href>`                                                | `apps/studio/src/App.tsx:2015`                                     |
| S8 | 运行时适配器带 `--dangerously-skip-permissions`/`bypassPermissions`/`--yolo` 类开关，任务 prompt 由 API 输入拼接，隔离仅靠工作区约定（设计级风险，非实现缺陷） | `packages/agent-runtime/src/index.ts:139`、`:164`、`:169`、`:213-222` |

### 3.3 低危

| #   | 发现                                                                                | 出处                                                                                      |
| --- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| S9  | 凭证明文落盘 `~/.agent-dev/credentials.txt/.json`（文件权限 0600、目录 0700 正确），无加密             | `packages/provider-cli/src/credentials.ts:28-38`、`secret-backend/local-file.ts:209-214` |
| S10 | infisical 后端把 `KEY=VALUE` 放进 argv，同机进程列表可见                                        | `packages/provider-cli/src/secret-backend/infisical.ts:63-65`                           |
| S11 | `generateEnvFile` 把 webhook secret 等写入工作区 `.env`（0600 且被生成的 `.gitignore` 排除，风险有限） | `packages/provider-cli/src/env-generator.ts:12`                                         |
| S12 | `GET /api/update/check` 在读操作上执行 `git fetch origin`（副作用）                           | `apps/daemon/src/app.ts:237`                                                            |

### 3.4 确认干净的方面

* 全库无硬编码 secret（`ghp_`/`sk-`/`AKIA`/私钥模式无命中）；`.gitignore` 覆盖 `.env`、`.agent-dev`、sqlite、`*.key`；`SECURITY.md` 无明显缺口。

* 无 `eval`/`new Function`/动态 import 拼接；所有子进程均为 `execFile`/`spawn` 数组参数、无 shell、无参数拼接。

* 无路径穿越（无静态文件服务、无基于外部输入的文件路径端点）；无 SSRF（fetch 目标均为固定域名或内部派生值）。

* MCP 桥接合规：21 个工具不暴露 approve/写入外部平台类操作；确认字面量服务端 `z.literal` 强制；`projectId` 已 `encodeURIComponent`；输入有 zod 校验（`apps/cli/src/mcp.ts`）。

* Studio 无 `innerHTML`/`dangerouslySetInnerHTML`。

## 4. 代码质量发现

| #  | 发现                                                                                                                                                                                        | 出处                                                                                 |
| -- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| Q1 | `advanceDelivery` 状态机隐患：xstate 对非法事件静默忽略、不校验迁移是否发生、两张表的写入不在同一事务                                                                                                                           | `packages/storage/src/index.ts:1555-1573`                                          |
| Q2 | 4 条 runtime 门禁路由绕过 zod，用裸字符串比较 + `as` 断言，其中 `:819` 的 `agentId` 完全未校验                                                                                                                      | `apps/daemon/src/app.ts:819`、`:850`、`:863`、`:876`                                  |
| Q3 | secret-backend 子系统整体孤儿化：约 615 行实现 + 9 条路由，无 Studio/MCP/测试消费者，零测试；它同时是 S4 的载体                                                                                                              | `packages/provider-cli/src/secret-backend/`、`app.ts:282-398`                       |
| Q4 | 死代码：`buildCodexExecutionPlan` 已被 `buildAgentExecutionPlan` 取代（仅自身测试引用）；`formatDoctorSummary`、`isPipelineBlocked` 等导出无生产消费者；`detectDrift` 四个适配器全部实现但仅测试调用                                  | `packages/agent-runtime/src/index.ts:109`、`packages/provider-core/src/index.ts:68` |
| Q5 | 一致性：确认字面量三处独立定义（`app.ts:30-114`、`mcp.ts:28`、`App.tsx` 约 12 处）；`'codex'` 魔法字符串 8 处（`app.ts:424/806/812/840` 等）；schema 列名 `status` 与 API 字段 `state` 不一致                                     | `packages/storage/src/schema.ts:7`                                                 |
| Q6 | 测试缺口：secret-backend 零测试；storage pipeline 执行（resume/isStepApproved，`index.ts:1179-1240`）零测试；Studio 零测试；MCP 21 工具仅 5 例                                                                      | —                                                                                  |
| Q7 | 文档漂移：`handoff.md:84` 称 ReleaseComposer 7 步，实为 9 步（`release.ts:79-89`）；版本号硬编码 `0.1.0-alpha.0`（`app.ts:218` 及各 package.json）与 v0.2 Pilot 定位脱节；Studio 类型含 `cancelled` 状态（`types.ts:264`）而后端无 | —                                                                                  |
| Q8 | 根目录 `install.sh`（153 行）与 `scripts/install-macos.sh`（273 行）是两套不同实现；文档（`implementation-plan-v0.2.md:62`）与 `npm run install:macos` 只承认后者，前者无任何引用，疑似遗留副本                                      | 仓库根                                                                                |
| Q9 | `execFileAsync('rm', ['-rf', …])` 依赖 Unix `rm` 二进制                                                                                                                                        | `packages/storage/src/index.ts:686`                                                |

遗留标记：源码中几乎为零（唯一一处在生成模板的脚手架注释 `packages/blueprint/src/generate.ts:488`，有意保留）；61 处 TODO 集中在文档（`docs/credential-management.md` 21 处为未落地草案等），属规划性内容。

## 5. 正面确认（不应在整改中被破坏）

* 所有子进程调用均为参数数组、无 shell 字符串拼接——命令注入面极小，整改时保持此纪律。

* MCP 桥接的边界设计（不暴露 approve 类、确认字面量服务端写死、凭证值不出本机）经逐工具核对与文档一致。

* 无空 catch；凭证文件权限位正确；状态机与凭证主路径有测试覆盖；web-saas→web-app 改名迁移干净（仅存有意迁移 `packages/storage/src/migrations.ts:93-103`）。

## 6. 整改方案

按优先级分五批。每批内部可独立提交；P0 完成前不分发安装脚本给外部用户。

### 6.1 P0 网络边界（阻断项，Pilot 前必须完成）

1. ✅ **绑定回环（2026-08-31 完成）**：`apps/daemon/src/index.ts` 的 `serve()` 已加 `hostname: '127.0.0.1'`。实跑验证：`netstat` 仅 `127.0.0.1:3799` 监听、回环请求 200、局域网地址（192.168.31.69）请求被拒绝；daemon 22 项测试全绿。
2. ✅ **本机 token 鉴权（2026-08-31 完成）**：daemon 启动时经 `loadOrCreateDaemonToken()` 生成 64 位十六进制随机 token，持久化到 `~/.agent-dev/daemon-token`（0600，目录 0700，路径可用 `AGENT_DEV_DAEMON_TOKEN_PATH` 覆盖；token 持久复用，daemon 重启不轮换，Studio/MCP 启动时读取一次即可）。`createDaemonApp` 新增 `authToken` 选项，`startDaemon` 恒传入；中间件对全部 `/api/*` 校验 `Authorization: Bearer <token>`（timingSafeEqual 比较），豁免清单显式且仅两项：`/api/health`（存活探针）与 `/api/github/webhooks`（GitHub 调用，走自身 HMAC 签名验证）；`/events` SSE 在 `/api/*` 之外（EventSource 无法带自定义头，只承载交付事件元数据）。Studio 侧由 Vite 启动时读 token 文件注入 `__DAEMON_TOKEN__`，`daemon-auth.ts` 包装 `window.fetch` 统一附加头；MCP 桥接从同一文件（或 `AGENT_DEV_DAEMON_TOKEN` 环境变量）读取并携带。配套 6 个契约测试（无/错/畸形 token 401、合法 token 通过、credential/secret/update 路由 401 回归、health/webhook 豁免、token 文件创建与复用），daemon 22 例全绿。
3. ✅ **URL scheme 白名单（2026-08-31 完成）**：新增 `httpUrlSchema(label)` helper（`app.ts`），在 `z.string().url()` 之上用 `new URL()` 解析并仅放行 `http:`/`https:` 协议。已套用到全部四处会到达 git 或 UI 的 URL 输入：`importRepositoryUrl`（apply 导入，S3——`ext::`/`file://`/`git@`/`ssh://` 在 schema 校验层 400，早于 `git clone` 执行）、PR 证据 `url`（S7——`javascript:`/`data:`/`file://` 无法写入，堵死存储型 XSS）、Preview 证据 `apiUrl`/`webUrl`。daemon 源码中不再有裸 `z.string().url()`。Studio 侧其余 `a.href` 赋值核实均为 `URL.createObjectURL(blob)` 下载链接，非注入面。配套 4 个契约测试（ext::/file/ssh/git@ 拒绝、http(s) 通过、javascript:/data: PR 证据拒绝且 https 通过 schema 到达交付闸门 409、preview 证据双向拒绝）。
4. ✅ **门控** **`/api/update`（2026-08-31 完成，采取移除方案）**：`POST /api/update` 与 `GET /api/update/check` 两条路由整体删除（S2、S12 一并消除）——前者是 `git pull` + `npm install` + `build` 的 RCE 面，后者在名义只读路由上执行 `git fetch`。更新本仓库改为用户显式动作：停 daemon → `git pull` → 重启。MCP 桥的 `agent_dev_check_update` 工具随端点一并移除（21 → 20 工具），`docs/mcp-bridge.md` 同步。Studio 本就无引用。daemon 测试新增「带合法 token 请求两条路由均 404」回归（证明是移除而非仅靠 token 门控），MCP 工具清单断言更新。secret-backend 去留（6.3-1）不在本条范围，仍待决策。

### 6.2 P1 状态机与校验一致性

1. ✅ **`advanceDelivery`（2026-08-31 完成）**：两张表（`deliveryRuns` + `projects`）的写入包进同一 SQLite 事务（`BEGIN IMMEDIATE`/`COMMIT`/`ROLLBACK`）；`send` 前后逐事件比较状态值——无转换的事件显式抛错 `Event X is not allowed in delivery state Y` 而非静默吞掉。唯一例外是**重放**：事件的目标状态已是当前状态（如恢复路径上 `BASELINE_CREATED` 在已达 `BASELINE_READY` 的 run 上重发）视为幂等，不抛错——这同时修复了实施中发现的回归：恢复场景重放 `BASELINE_CREATED` 曾导致步骤全绿但 run 被误标 `failed`。重放判定经 workflow 包新增的 `isEventReplay()`（事件→目标状态静态映射，`PAUSE`/`RESUME`/`FAIL`/`RETRY` 不在映射中，无操作即抛错）。回归测试：「非法事件被拒且状态不变」+「重放幂等」（`packages/storage/test/index.test.ts`）。
2. ✅ **runtime 路由 zod schema（2026-08-31 完成）**：`runtime/run`（含 `agentId` trim+长度 1-120 约束）、`runtime/execute`、`runtime/retry`、`runtime/cancel` 四条路由全部改用 zod schema 与确认字面量（`app.ts`），与其余路由写法统一；手写 body 解析全部移除。
3. ✅ **未 await 的** **`persist()`（2026-08-31 完成）**：`reviseBlueprint` 改为 `async` 并 `await this.persist()`（原为同步函数内调用 async persist 不返回 Promise，磁盘落库从未被等待）；daemon 两处调用点补 `await`。

### 6.3 P2 清理与一致性

1. ✅ **secret-backend 去留决策（2026-08-31 完成，用户拍板：移除路由、保留库）**：`app.ts` 的全部 9 条 `/api/secret-backend/*` 管理路由整体删除（S4 明文出口消除）——事实核查确认零消费者：Studio 无 UI、交付管线走 `/api/credentials` 凭证系统、MCP 桥从未暴露。`packages/provider-cli/src/secret-backend/` 库（local-file/Infisical/registry，含版本/审批/轮换/历史设计，26 个库测试保留）作为 P1-2 Infisical Adapter 的地基保留；P1-2 落地时路由随测试与文档一起回归。回归测试更新：带合法 token 请求 `keys`/`:key` 均 404（证明是移除而非仅靠 token 门控）。双凭证系统并存问题随之消除。
2. 删除根目录遗留的 `install.sh`（或合并进 `scripts/install-macos.sh`），消除双入口歧义。
3. 清理死代码：`buildCodexExecutionPlan`、`formatDoctorSummary`、`isPipelineBlocked` 等；`detectDrift` 保留（接口契约，四适配器已实现，后续会消费）。
4. 确认字面量收敛为单一导出常量，`app.ts`/`mcp.ts`/Studio 三处引用同一事实源。
5. 文档与版本对齐：`handoff.md:84` 的 7 步改 9 步；`/api/health` 版本号改为从 `apps/daemon/package.json` 读取（对齐 MCP 桥的做法）；决策是否升版本号为 `0.2.0`。
6. `rm -rf` 改为 `node:fs/promises` 的 `rm({ recursive: true, force: true })`（`index.ts:686`）。

### 6.4 P3 Windows 兼容（2026-08-31 完成）

1. ✅ **npm/npx 调用统一处理**：四处调用点（`storage/src/index.ts` 的 quality gate 与依赖安装、`agent-runtime/src/doctor.ts` 的 npm 版本与 wrangler 检查）统一加 `shell: process.platform === 'win32'`（导出为 `npmSpawnOptions` 常量）。注意不能直接 spawn `npm.cmd`——Node ≥18.20/20.12/22 因 CVE-2024-27980 修复对无 shell 的 `.cmd` spawn 抛 `EINVAL`；各调用点参数均为固定字面量，`shell: true` 无注入面。
2. ✅ **信号用例（2026-08-31 完成）**：`agent-runtime/test/index.test.ts` 的 SIGTERM 强杀用例改为平台分支断言——Windows 上断言「进程被终止」（`timedOut === true` 且 `exitCode !== 0`，Windows 的 kill 无 POSIX 信号），POSIX 上保持 `signal === 'SIGKILL'`。
3. ✅ **符号链接用例（2026-08-31 完成）**：storage 的 symlink 守卫用例捕获 `EPERM`（无开发者模式/管理员权限的 Windows 拒绝创建符号链接）后 skip（`return`），守卫本身平台无关；其余错误照常抛出。

**效果**：Windows 全仓测试从「storage 2 例 + agent-runtime 1 例稳定失败」变为全绿（storage 14/14、agent-runtime 单独运行通过）；全量并行时 storage 的 recovery 用例偶发资源竞争 flake，单独运行稳定通过（负载敏感，非回归）。

### 6.5 P4 测试补齐

1. storage pipeline 执行（resume/`isStepApproved`）补测试。
2. MCP 桥补测试至覆盖全部 21 工具的关键路径（现有 5 例偏薄）。
3. `advanceDelivery` 事务化后，补「非法事件被拒」「写入原子性」两条回归测试。
4. Studio 关键视图至少补渲染冒烟测试（当前为零）。

## 7. 验收标准

* ✅ `netstat`/`lsof` 确认 daemon 只监听 `127.0.0.1`（2026-08-31 实跑验证，测试端口 3799）；

* 不带 token 的请求对全部 `/api/*` 路由返回 401（含 `/api/update`、`/api/secret-backend/*`，若仍存在）；

* `importRepositoryUrl` 传 `ext::`/`file://` 被 400 拒绝，并有对应测试；

* `npm test` 在 Windows 与 macOS 均为全绿；

* 本文件各批条目逐项标记完成，`handoff.md` 同步。

